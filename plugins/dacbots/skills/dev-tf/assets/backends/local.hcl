# Selected by BACKEND=<id> (see BACKEND_MAP in the Taskfile). Copy this into
# <stack>/variables/<id>.backend.hcl and fill in path. tf_setup mkdir -p's
# its parent directory before the first init; the file this path names *is*
# the state, so never delete it without confirming first.
path = ""
